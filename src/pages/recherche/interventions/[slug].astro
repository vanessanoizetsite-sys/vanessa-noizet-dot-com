---
import MainLayout from '#layouts/main-layout.astro'
import { getStaticPathsFromCollection } from '#utils/get-static-paths-from-collection.ts'
import { render } from 'astro:content'
import { css } from 'styled-system/css'

export const getStaticPaths = getStaticPathsFromCollection('interventions')
const post = Astro.props
const { Content } = await render(post)
---

<MainLayout title={post.data.title} description={`Intervention « ${post.data.title} » par Vanessa Noizet.`}>
	<article>
		<h1 class={css({ textStyle: 'heading-1', fontWeight: 'bold', color: 'default' })}>
			{post.data.title}
		</h1>
		<Content />
	</article>
</MainLayout>
